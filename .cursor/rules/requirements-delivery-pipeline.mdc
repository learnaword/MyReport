---
description: 需求设计按用户点名执行：需求可为详细文档或一段话；一次只做指定项；审查前不编码
alwaysApply: true
---

# 需求设计：用户点名制（非流程化）

## 原则

1. **不流程化**：不要按固定顺序自动推进，也不要默认「下一步该做 xxx」。
2. **用户点名**：只做用户**明确指定**的那一项（如「做 PRD 分析」「做 API 设计」）。
3. **一次一项**：本轮只产出该项文档/结论；做完即停。
4. **未点名不动**：用户未指定时，先列出可选能力菜单并询问做哪一项，**不要自行开始**。
5. **确认前不编码**：除非用户明确说「开始编码 / 实现 xxx」。
6. **需求输入两种均可**（详细文档 **或** 一段话/一句话），见下节。

## 需求输入

| 形态 | 说明 | Agent 做法 |
|------|------|------------|
| **详细文档** | 长文粘贴、教程/规格、或已有 md 路径 | 以该文档为输入做用户点名的项；尊重原文；冲突与缺口标「待确认」，勿无视已有内容另起无关稿 |
| **一段话 / 一句话** | 如「支持按学校维度合并报告」 | 仍须用户点名要做哪项；做 PRD 时负责展开；做其它项时文首写「需求来源：一段话 ……」，缺信息列「待确认」，勿当成已拍板的完整规格 |

用户只给了需求、**未点名能力**时，回复：

```text
需求已收到（详细文档 / 一段话）。
请指定要做哪一项，例如：
- 做 PRD 分析
- 做技术方案
- 做数据库设计
- 做 API 设计
- 拆 Task
- …
功能短名建议：<name>（可改）
```

## 产出路径约定

功能短名 `<name>` 用小写+下划线（如 `create_report`）。同一功能的设计文档都放在同一目录：

```text
docs/<name>/
  prd_<name>.md
  tech_design_<name>.md
  db_design_<name>.md
  api_design_<name>.md
  tasks_<name>.md
```

编码规范、项目 Context 仍可为全局：`docs/coding_standards.md`、`AGENTS.md`、`.cursor/rules/`。

## 可选能力（菜单，无强制顺序）

| 能力 | 用户可怎么说 | 产出路径 |
|------|--------------|----------|
| PRD 分析 | 做 PRD 分析 | `docs/<name>/prd_<name>.md` |
| 技术方案 | 做技术方案 | `docs/<name>/tech_design_<name>.md` |
| 数据库设计 | 做数据库设计 | `docs/<name>/db_design_<name>.md` |
| API 设计 | 做 API 设计 | `docs/<name>/api_design_<name>.md` |
| Task 拆分 | 拆 Task / 任务拆分 | `docs/<name>/tasks_<name>.md` |
| 编码规范 | 对齐/整理编码规范 | `docs/coding_standards.md` 或更新规则 |
| 项目 Context | 更新项目 Context | `AGENTS.md`、`.cursor/rules` |
| 编码实现 | 开始编码 / 实现某某 | 代码（仅此项） |

对应 ECC：`plan-prd`/`prp-prd`、`plan`/`architect`/`blueprint`、`database-reviewer`、`api-design`、`coding-standards`、`java-coding-standards` 等——**仅在用户点名的那一项上启用**。

## 结束时怎么说

```text
本项（{用户指定的能力}）已完成，请审查。
若需修改，直接说明改点（仍做本项）。
若要做其它项，请直接指定，例如：「做数据库设计」或「开始按 tasks 编码」。
```

不要写「请进入下一步：xxx」。

## 修改当前产出

用户说「这里不对，改成…」→ **仍做同一项**，只改文档，不切换到其它项、不编码。

## 接口变更

若用户点名的是 API 设计且变更对外契约 → 遵守 `api-change-frontend-docs`；仍不自动去做 Task/编码。
