---
description: 对外接口变更时必须输出调用方修改文档到 docs/
alwaysApply: true
---

# 接口变更调用方文档规则

本仓库为报告生成 **后端服务**（无内置前端页面）。对外契约变更时，须为 **HTTP 调用方**（其它系统 / 前端项目）输出对接文档。

## 何时触发

涉及以下任一变更时，**在改代码的同一次交付中**必须新增或更新一份文档：

- 新增 HTTP 接口
- 修改请求参数 / 入站字段
- 修改响应结构 / 出站字段
- 修改异步约定（如进度 Redis key、状态码语义、落盘路径约定）

纯内部重构、不改变对外契约时可不写。

## 文档落盘

- 目录：`docs/`（可放在 `docs/` 根下，或 `docs/<feature>/`）
- 格式：Markdown
- 文件名：小写字母 + 下划线，例如 `docs/create_report.md`、`docs/report_progress.md`
- 若已有同名文档：在原文件上增量更新，不要另起冲突文件名

## 文档内容最低要求

1. **API 说明**：路径、方法、请求参数、响应格式  
2. **涉及的调用方**：已知对接系统 / 页面 / 客户端（若本仓库无前端，写清「外部调用方需改点」）  
3. **具体对接示例**：请求 JSON、成功/失败响应、进度轮询或 Redis 约定等可落地片段  

须覆盖下方模板中的章节。

## 文档模板

```markdown
# 功能名称

## 涉及调用方

- 系统 / 页面 / 客户端（说明）

## API

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /report/createReport | 提交报告生成任务 |

**请求参数：**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| reportId | Integer | 是 | 报告 ID |

**响应：** `{ "code": 0, "message": "报告生成任务已提交", "reportId": 1 }`

**异步约定（如有）：** Redis key / 进度字段 / 终态含义

## 调用方修改点

### 1. 请求示例

```json
{
  "reportId": 1,
  "reportName": "示例报告",
  "reportJsonArr": [],
  "overallSetting": {}
}
```

### 2. 响应处理

```js
// code === 0 表示任务已提交；需另行轮询进度
```

### 3. 进度 / 状态（如有）

```text
Redis key 模式、字段含义、失败态
```

## 注意事项

- 注意事项1
- 注意事项2
```

## Agent 执行要求

1. 先改后端契约，再写/更新 `docs/*.md`，再改本仓库其它层（或在同一 PR 中文档与代码齐全）。  
2. 回复用户时写明文档路径，例如：`已输出调用方修改文档：docs/create_report.md`。  
3. 若存在 `docs/README.md`，同步核对是否需要增加该文档链接。  
4. 架构级协议变更时，同时更新根目录 `AGENTS.md` 中的 API / 流程摘要。
