---
description: MyReport 项目开发约定与协作规范
alwaysApply: true
---

# 项目开发约定

## 概述

本规则定义 MyReport 的开发模式与协作约定。更详细的目录结构、模块划分见 `AGENTS.md`。

## 技术栈

- **语言**: Java 8（`maven.compiler.source/target=1.8`）
- **架构**: Spring Boot 2.7.18 单体 JAR + Spring MVC REST（本仓库无 JSP / 前端页面）
- **持久化**: Spring Data JPA + MySQL 8（`entity` / `repository`）
- **缓存 / 进度**: Jedis（`framework.redis`，非 Redisson / 非 Spring Data Redis 模板）
- **Word**: Spire.Doc（`lib/` system 依赖）+ Apache POI
- **JSON**: Fastjson
- **测试位置**: `src/test/java`

## 分层与包约定

- 当前主路径：`controller → util.word（业务）→ framework.redis / 落盘`
- 新增 HTTP 接口放 `controller`；请求体放 `vo`
- 新增持久化：`entity` + `repository`；可按需抽 `service`，勿平行再造 Redis 客户端
- 报告渲染逻辑优先落在 `util.word` / `util.word.common`，表格相关放 `common.table`
- 优先复用 `framework` 与现有 `Constant.RedisKey`，不随意新建平行体系
- 响应风格与现有接口对齐：`code`（0 成功 / -1 失败）+ `message`（除非明确另定契约）

## 提交约定

遵循约定式提交（Conventional Commits）：

### 前缀

- `feat` - 新功能
- `fix` - 错误修复
- `docs` - 文档变更
- `test` - 测试添加或更新
- `refactor` - 代码重构
- `style` - 代码风格（格式化、分号等，不含逻辑变更）
- `chore` - 构建、依赖、工具变更

### 消息规范

- 第一行简洁（建议 < 70 字符），使用祈使语气
- 可带范围：`feat(report): 支持按届次过滤客观题`
- 示例：`fix(redis): 报告生成失败时清理 REPORT_LOCK`

## 代码风格

- 遵循 Java 命名约定；保持 Java 8 语法
- 现有工具类多为静态方法风格，新增同模块代码宜保持一致；新 Spring Bean 可用构造注入或字段注入，与邻近代码一致即可
- 变量和方法名语义清晰；方法保持单一职责
- 注释只写非显而易见的业务逻辑，避免冗余 Javadoc
- 最小改动原则：不顺手升级 Spring Boot / JDK / Spire；不批量无关重构

## 测试

- 改动业务逻辑时至少执行 `mvn -DskipTests compile`（或 `make build`）
- 新增或修改易错逻辑时补充有意义的单元测试（`src/test/java`）
- 涉及 MySQL、Redis、文件落盘的功能需结合本地 `make infra-up` 手动验证
- 不追求虚高覆盖率；优先覆盖核心路径与易错逻辑（进度/锁、异步失败清理）

## 编译与文档

- 跨层改动后执行编译检查：`make build` 或 `mvn -DskipTests compile`
- 接口变更（路径、参数、响应）须输出调用方文档到 `docs/`，见 `api-change-frontend-docs.mdc`

## 合并请求规范

- 保持 PR 专注且小，一次解决一类问题
- 新功能附带必要测试与编译验证
- 接口变更同步 `docs/` 文档
- 合并前自查：编译通过、无敏感信息误提交、未改动无关 `.idea` 文件、未误提交 `.env`
