---
description: MyReport 安全护栏（SQL/XSS/密钥/输入与错误处理）
alwaysApply: true
---

# 安全护栏

## SQL 注入防护

- 永远不要将用户输入直接拼接到 SQL 查询中。
- 始终使用参数化查询或 JPA / 预编译方式。
- 在数据库操作前验证和清理所有输入。

## XSS / 文档注入

- 本服务主要产出 Word，而非 HTML 页面；仍须警惕将未校验用户内容写入文档/文件名导致路径穿越或异常膨胀。
- 报告名、路径片段、模板选择等用户可控字段做校验与清理，禁止 `../` 等路径穿越。

## 密钥管理

- 永远不要在源代码中硬编码密钥、API 密钥或密码。
- 使用环境变量（见 `.env.example`）存储 `MYSQL_*`、`REDIS_*` 等敏感配置。
- 勿提交真实 `.env`；避免在日志中打印密码、完整连接串中的口令。

## 输入验证

- 在系统边界（Controller / VO）验证必填字段（如 `reportId`）。
- 使用白名单而非黑名单进行关键校验。
- 处理模板、上传或远程资源前检查类型与合理大小上限。

## 错误处理

- 不要在对外 `message` 中暴露堆栈、本机绝对路径或内部凭证。
- 在内部用 Logger 记录详细错误，向调用方返回通用/可行动的失败信息。
- 异步报告任务须捕获异常并更新 Redis 进度 / 清理锁，避免静默失败。

## 依赖管理

- 不顺手升级核心依赖；变更 Spire / Spring Boot 需明确评估。
- 审计依赖项中的已知漏洞。
- 固定版本以避免意外的破坏性变更；`lib/` 中的 system JAR 变更须同步 `pom.xml`。
